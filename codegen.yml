overwrite: true
schema: "http://localhost:9000/graphql"
documents: "src/shared/apollo/queries/*"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
