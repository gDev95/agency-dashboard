import styled from "styled-components";
import { List } from "@material-ui/core";

const StyledMaterialList = styled(List)`
	max-width: 100%;
	background-color: #fff;
	text-decoration: none;
`;

export default StyledMaterialList;
